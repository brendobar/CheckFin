import React from 'react';
import AuthError from "@/app/auth/ui/error/authError";

const Page = () => {
    return (
        <div>
            <AuthError/>
        </div>
    );
};

export default Page;