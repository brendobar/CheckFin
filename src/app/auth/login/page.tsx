import React from 'react';
import AuthForm from "@/app/auth/ui/form/authForm";

const LoginPage = () => {
    return (
        <div>
            <AuthForm type='login'/>
        </div>
    );
};

export default LoginPage;